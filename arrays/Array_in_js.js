// ways of declaring arrays:

const myArr = [0, 1, 2, 3, 4]

const myName = ["Hafiz", "Muneeb"]

const myArr2 = new Array(1, 2, 3)

myName.push("Afzal"); // .push() method add a value in an array

myName.pop(); // .pop() method remove the last one element present is an array

const newName = myName.join('-');

// console.log(newName);

const heros = ["hafiz", "muneeb"]

const heros2 = ["Afzal", "haseeb"]
heros.push(heros2);
console.log(heros);
